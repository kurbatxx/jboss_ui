import 'package:flutter/material.dart';

// tabbar
const kTabBarHeight = 36.0;
const kTabSize = Size(200.0, 32.0);
const kMinimumRadius = BorderRadius.all(Radius.circular(4.0));
const kZeroRadius = BorderRadius.all(Radius.circular(0.0));
